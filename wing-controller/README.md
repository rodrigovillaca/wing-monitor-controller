# wing-controller

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build wing-controller` to build the library.

## Running unit tests

Run `nx test wing-controller` to execute the unit tests via [Vitest](https://vitest.dev/).
